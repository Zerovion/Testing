# The "bridge" to the backend. Set this to your deployed backend's URL
# (e.g. https://your-backend.onrender.com) — no trailing slash.
# Leave empty for local dev if you're using the Vite proxy below.
VITE_API_URL=
